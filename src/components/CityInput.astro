---
---

<!-- City Input Form -->
<section id="plant" class="city bg-white/50">
  <input type="text" id="city-input" placeholder="Enter city name" />
  <button id="city-search-button">Search</button>
</section>

<script>
  const citySearchButton = document.getElementById('city-search-button') as HTMLButtonElement;
  const cityInput = document.getElementById('city-input') as HTMLInputElement;

  const searchCity = () => {
    const cityName = cityInput?.value.trim();
    if (cityName) {
      // Dispatch a custom event with the city name
      const event = new CustomEvent('citySearch', { detail: { cityName } });
      window.dispatchEvent(event);
    }
  };

  citySearchButton?.addEventListener('click', searchCity);

  cityInput?.addEventListener('keypress', (event) => {
    if (event.key === 'Enter') {
      searchCity();
    }
  });
</script>

<!-- Styling -->
<!-- <style>
  section {
      position: absolute;
      left: 7%;
      top: 35%; /* This remains as a percentage */
      transform: translate(-50%, -50%);
      padding: 1.25em; /* 20px */
      border-radius: 0.625em; /* 10px */
      box-shadow: 0 0.25em 0.5em rgba(0, 0, 0, 0.1); /* 4px 8px */
  }
</style> -->